# 家族族谱 · 可编辑传代树 —— 项目交接文档（Handoff）

> 写给接手本项目的 Agent / 开发者。读完这份文档 + 打开一次 HTML，即可完整接手。
> 交接时间：2026-08-26 · 作者：墨语（WorkBuddy Agent）· 归属：豪哥（zax）的个人项目
> **v13 深度重构**：2026-08-27 · 接手 Agent 完成 bug 修复 / 结构重构 / 视觉重设计 / 自动化回归套件（见 §11 版本时间线与 §8 新增教训）

---

## 1. 项目一句话

把一张**手写族谱照片**转成**单文件、可编辑、可导出**的家族传代树网页：
- 单个 HTML 文件，零依赖、零服务器、断网可用，双击浏览器打开即用
- 数据存在 HTML 文件本身（内嵌 JSON），换浏览器/换电脑打开同一文件数据一致
- 支持：录入成员（姓名/多配偶/出生年/卒年/备注）、自动按出生年排长幼、手动拖拽排序、折叠/展开、导出 JSON/MD、打印

## 2. 文件清单

| 文件 | 作用 | 备注 |
| --- | --- | --- |
| `index.html` | **主交付物**（纯应用 + 初始数据种子，含 40 代字辈表） | 唯一需要维护的文件（当前为 v15）。**v15 起文件不再被写入；数据真源 = 用户浏览器的 localStorage（zupu_data_v4）。内嵌 JSON 是种子/初始数据（当前为用户真实族谱：周姓，字辈勤11/厚14），改代码绝不能动数据块；测试走 tests/ 夹具（示例数据副本）** |**注意：内嵌 JSON 已是用户的真实族谱数据（周少群/周勤善等），不是示例数据——改代码时绝不能动数据块；测试用 tests/ 夹具（示例数据副本）** |
| `data.json` | 数据快照（从 HTML 内嵌 JSON 提取） | 备份/迁移用，改数据后可用「📂 导入 JSON」载回 |
| `家族族谱-思维导图.md` | 当前数据的 Markdown 大纲 | 供 XMind/Obsidian 等导入 |
| `tests/` | **Playwright 自动化回归套件**（v13 新增） | `runner.py` 一键跑全部 17 个用例；`shots.py` 出视觉截图 |
| `handoff.md` | 本文档 | |

## 2.5 Git 仓库（v15.29 起）

- 远端：`https://github.com/zaxchou/zupu.git`（分支 main，已绑定 upstream）
- 每轮改动完成、全套测试绿之后：`git add -A && git commit && git push`
- `.gitignore` 排除：tests/_out、__pycache__、README-解压说明.txt、过时快照 家族族谱-思维导图.md、历史调试脚本 tests/_p*.py
- GitHub Pages demo 站已启用：https://zaxchou.github.io/zupu/ （main 分支根目录，push 自动部署；访客数据存各自浏览器）
- 内嵌种子已通用化（赵钱孙李 demo，v15.30）；周家数据在 samples/ 与用户本机浏览器
- 仓库 public：samples/ 的周氏示例是用户确认可公开的样例

## 3. 快速上手

- **运行**：直接用浏览器打开 `index.html`（file:// 协议即可，无需任何服务器/构建）
- **编辑**：点节点下方常驻小「＋」打开操作菜单；双击姓名行内改名；双击姓名下方小字行开档案弹窗
- **保存（v14.4 起，Gmail 式无感）**：首次点保存直接弹一次文件选择器（无任何说明弹层），选中族谱文件即绑定；此后编辑约 1 秒自动覆盖写回、Ctrl+S 立即保存，**例行保存零提示**，状态只有顶栏「已保存/未保存」两态；Firefox/Safari 退回下载「家族族谱.html」
- **测试**：系统 Python `C:/Python314/python.exe` 已 pip 安装 playwright，headless Chromium 用本机缓存；`cd tests && python runner.py`（需先起 `python -m http.server 8931`，见 §9）

## 4. 数据架构（重要，先读这个）

设计原则：**数据只有一份真源（Single Source of Truth）＝ HTML 文件本身**。

```
[HTML 文件]
  └─ <script id="__treeData" type="application/json">…族谱 JSON…</script>   ← 数据真源
        ↑ 打开文件时读取（任何浏览器一致）
        │
        ├─ localStorage 草稿（键 = zupu_tree_data_v3_<内嵌内容哈希>，v14.1 起按文件隔离；
        │     旧共用键 zupu_tree_data_v3/v2 只读迁移）  ← 仅"意外刷新草稿"，不是真源
        │
        └─ 保存（v15，Gmail 式真·无感）：
              数据真源 = 本浏览器 localStorage（键 zupu_data_v4），每次编辑同步写入，
              零弹窗、零下载、零文件写入。载入时浏览器没有数据（首次/换电脑）则在
              「文件内嵌 JSON / 旧版草稿」里挑成员最多的一份做初始（自动抢救）。
              备份 = 「文件 ▾ → ⬇️ 备份到文件」下载带日期 json（唯一出口，纯下载）；
              恢复 = 「📂 恢复备份」支持 json 与旧版 html（自动抽出内嵌数据）。
              换电脑 = 备份 json → 新机器恢复。文件内嵌 JSON 仅作首次种子，之后不再更新。
```

加载优先级：**内嵌 JSON（≠初始模板时是唯一真源）→ 真草稿（仅当文件还是初始模板）→ 内置初始数据**。

草稿键规则（v14.1）：草稿键 = `zupu_tree_data_v3_` + 内嵌 JSON 文本的 djb2 哈希。同一文件 → 同一槽位，刷新/重开自动恢复实时编辑；**不同族谱文件各有各的槽位，互不污染**（v12/v13 全浏览器共用一键，A 文件的草稿会泼到 B 文件上——真实设计缺陷，已修）。顶栏「固化状态灯」实时显示数据是否已写回文件：绿=已固化，橙=仅在本浏览器草稿里（点灯即保存）。

关键函数（均在 HTML 的 `<script>` 内，v13 起按编号分区：①工具 ②数据层 ③历史栈 ④渲染 ⑤行内编辑 ⑥档案弹窗 ⑦气泡菜单 ⑧编辑动作 ⑨拖拽 ⑩缩放平移 ⑪搜索 ⑫导入导出打印 ⑬键盘总控）：
- `INIT_TEMPLATE`：**直接派生自内嵌 JSON**（v13 删除了 JS 里的第二份 initialData，杜绝双份漂移）；内嵌缺失/损坏时退化为空白根，可「导入 JSON」救回
- `loadData()`：按上述优先级加载
- `saveData()`：每次 render 后写 localStorage 草稿（**跳过与初始模板相同的数据**）
- `buildSavedHtml()`：在 document 克隆上替换内嵌 JSON 后返回完整 HTML 字符串（不污染活动 DOM；**函数式 replacement** 杜绝 `$` 序列注入；`textContent` 赋值不受属性顺序影响）
- `saveToFile()`：调 `buildSavedHtml()` 下载，**文件名带日期时间**（`家族族谱-YYYYMMDD-HHMM.html`），避免多次保存互相覆盖
- `sanitize()`：序列化前剥离布局残留字段（`_w/_x/_y`），**顶层先判 `typeof !== 'object'` 直接返回**（字符串原样，否则会把配偶拆成逐字对象）
- `migrate()`：加载时兼容旧格式（`spouse` 单值 → `spouses` 数组；补 `birth/death/note` 默认值；剥离 `_` 前缀字段）
- `pushHistory()/undo()/redo()`：操作历史栈（v13 新增，提交点只在真正改数据的动作；同节点连续折叠 1 秒内合并）

⚠️ 接手者必须遵守的规则：
1. **不要**再引入数据库/后端 —— 需求明确"不需要太复杂的数据库"，单文件方案是刻意选择
2. **不要**再在 JS 里复制一份初始数据 —— v13 起 `INIT_TEMPLATE` 从内嵌 JSON 派生，改初始数据只改内嵌 JSON 一处
3. 内嵌 JSON 里**不能出现字面 `</script>`**（会截断 script 标签），序列化前必须 `replace(/<\//g,'<\\/')`；**JS 注释里也不能写 `</script>`**（历史教训，见 §8）
4. `render()` 里 `layout()` 会把 `_w/_x/_y` 写到数据对象上 —— 所有持久化（localStorage/文件/导出）必须走 `sanitize()`
5. 全局键盘快捷键入口必须先过「搜索框 > 弹窗 > 输入型目标（INPUT/TEXTAREA/contentEditable）> defaultPrevented」分层守卫，再进快捷键分支（v12 泄漏教训，见 §8.10）
6. 需要在显示前测量尺寸的浮层，必须先 `display:block` 再读 `offsetWidth/Height`（display:none 下恒为 0）

## 5. 数据模型

```js
// 每个节点（人）：
{
  id: "b1a",                    // 唯一 id（genId 生成 "前缀_时间戳36进制_序号"）
  name: "雷志成",                // 姓名
  spouses: ["俞贵玄（美?）"],     // 配偶数组（支持多位：元配/继室/侧室）
  birth: "1920",                // 出生年，可写 "1920" / "约1900" / "1920?"
  death: "1985",                // 卒年（在世留空）
  note: "备注文本",              // 备注（官职/迁徙/过继等）
  gender: "f",                  // 性别：仅 "f"=女 会被标注（男存 ""，谱书惯例只标女）
  expanded: true,               // 是否展开（折叠状态也会被保存）
  children: [ …同构递归… ]       // 子女数组
}
```

排行规则（长幼）：
- 有 `birth`（能解析出 4 位年份）→ 同辈**自动按年份升序排**，年长在左，角标显示蓝色年份
- 无 `birth` → 按**手动拖拽次序**（最左为长），角标显示排行称谓（长/次/三/四…，`RANK_WORDS`）
- 混排：有年份的按年排前面，无年份的保持手动相对次序排后面

## 6. 功能清单（全部已实现并有自动化用例覆盖）

- 家庭传代树渲染：三遍法（建 DOM 实测宽度 → 递归布局 → 摆放 + 族谱肘形母线连线）
- 折叠/展开：菜单「折叠此支/展开此支」+ 工具栏全展开/全折叠 + **折叠支「▸ N」人数角标（点角标直接展开，v13）**
- 人员档案弹窗：姓名/出生年/卒年/备注（保存后同辈自动按生年重排）
- 多配偶：每位配偶独立显示/改名/移除（双击配偶文字）
- 排行角标：蓝色=出生年，橙色=排行称谓
- 气泡操作菜单：节点下方**常驻**小「＋」→ 点击式纵向菜单（配偶/子女/同辈/改名/档案/折叠/删除，根节点为 ＋第一代），鼠标移动不消失、点外部/Esc 关闭、相邻节点自动切换；**右键节点同样打开（v13）**
- 长幼拖拽：按住节点左右拖换位（8px 阈值防误触、整子树跟随、橙色虚线指示落点）；**独子也可拖**
- **拖动过继（v14.2）**：按住某人拖到另一张卡片上**停 0.4 秒**，目标亮琥珀光环，松手即把 TA（连同后代整支）挂为该人的子女（排最幼，目标自动展开）；拖到自己后代上显示红框拒绝（防成环）；Ctrl+Z 可撤销。悬停命中用 `nodeRects` 世界坐标数学计算（不依赖 elementFromPoint，拖动/缩放中都稳定）
- 缩放/平移（v13 重做）：Ctrl+滚轮**以光标为锚点**缩放、普通滚轮/触控板可平移、拖空白平移、适应屏幕/1:1；视口 `overflow:auto` + 隐藏滚动条
- **撤销/重做（v13）**：Ctrl+Z / Ctrl+Y / Ctrl+Shift+Z，栈深 100，删除可反悔
- **搜索定位（v13）**：Ctrl+F 或 🔍 按钮，搜姓名/配偶/生卒年/备注，高亮全部命中、计数、Enter/▲▼ 循环跳转并自动滚动居中
- **统计条（v13）**：工具栏右侧实时显示「N 位成员 · 配偶 M · D 代」
- 导出：JSON / Markdown / 打印 PDF（**打印生成文档式大纲，覆盖全部分支无视折叠，v13 修复打印截断**）
- 导入 JSON：可迁移旧数据（旧单配偶格式自动迁移；**导入前递归校验结构，v13**）
- 持久化（v15）：**数据真源 = 本浏览器**（zupu_data_v4，每次编辑同步写入，无感）；首次载入自动从文件内嵌 JSON 或旧版草稿抢救播种（挑成员最多者）
- **备份/恢复（v15）**：「⬇️ 备份到文件」纯下载带日期 json（零弹窗）；「📂 恢复备份」支持 json 与旧版 html；顶栏无保存按钮、Ctrl+S 只提示已自动保存
- **状态灯（v15）**：恒绿「已保存」（自动保存是常态，无需用户关心）
- **字辈定代（v14.6）**：根节点 JSON 增加 `zibei` 数组（40 代字辈字，随文件保存/迁移）。每张卡片最左侧显示「代数·字辈」角标：名字含字辈字=蓝标权威定代；未用字辈按父辈→同辈→世系偏移推算（灰标，tooltip 注明来源）；推算与字辈冲突时以字辈为准并提示「树中可能缺一代」。帮助弹窗含 40 格字辈全表（蓝框=在谱），统计条附「字辈第X–Y代」
- **性别标注（v15.12）**：档案弹窗「性别」选「女」→ 卡片姓名旁粉色「女」徽标（男不标，谱书惯例）；搜索列表、打印、MD/PNG/PDF 导出同步「（女）」
- **配偶谱书式（v15.12）**：显示层把「丁嘉」转成「丁氏嘉」（复姓两字识别、已含「氏」原样保留）；数据仍存原名，搜索按原名命中。设置弹窗可关
- **谱书显示设置（v15.12/15.13）**：视图 ▾ →「谱书显示设置…」弹窗三个选项：①世代基准（字辈第 1 字对应的谱书世数，填 13 → 勤字辈显示「23世·勤」；填 1 恢复「11代」）；②配偶谱式开关（丁嘉→丁氏嘉）；③**姓氏周显示开关（默认开；关 = 只报名：周勤善→勤善，卡片/搜索列表/打印/导出全走 `dispName()`，数据存全名）**。配置存本浏览器 `zupu_cfg_v1`（与数据分开），角标/搜索/统计条/帮助字辈表/画布导出全部同步换算，tooltip 注明「谱书世数，应用自算第 N 代」
- **谱序·堂号源流（v15.16）**：根数据 `clan` 字段（谱名/堂号/源流世系/先祖/始祖记/始迁祖/家族来源），顶栏品牌区显示谱名+堂号（点品牌打开谱序弹窗，视图 ▾ 也有入口）；标签页标题、PNG/PDF/打印/MD 导出标题自动带「谱名（堂号）」；载入时缺失自动从内嵌种子补齐（同字辈表救援逻辑）。内嵌种子已录入：潮阳泗水周氏族谱 · 申锡堂（周敦颐—焘—縯—良卿—宣道—景一—梅叟，一世承节公宣道、二世朝奉公景一、三世梅叟昆仲创基潮阳泗水；景珩支创基南海石塘）
- **过继标记（v15.14/15.18）**：档案「过继」下拉三态：嗣子（入继此支，琥珀实底）/ 嗣出（出继他支，虚线描边）/ 兼祧（一子继两房，蓝调双线）；tooltip 注明谱书式含义；打印/MD/世系录/画布导出同步
- **世系录（欧式五世一表）（v15.17）**：文件 ▾ →「世系录（五世一表）」下载独立 HTML：每人一行行传（世次/讳/字/号/行第/生/卒/配偶/子女/记），按谱世每五代一表，文首带谱序摘要；`exportShixilu()`
- **止（无传）标记（v15.19）**：档案勾选 → 卡片黑圈「止」徽标（谱书凡例），全链导出同步；手动标记不自动推断
- **配偶称谓细化（v15.20）**：`spRoles` 按人覆盖（配/继配/娶/聘/侧室），档案弹窗按配偶生成下拉，空=次序默认（原配/续弦；竖排=配/继配）；`spouseRoleAt(n,i)` 统一取称谓，仅存覆盖项向后兼容
- **字/号（v15.14）**：档案弹窗「字」「号」输入 → 卡片姓名后浅灰小字「字X」「号X」（竖排时独立列）；搜索可按字号命中；tooltip 显示「，字X，号X」；导出同步
- **世数用汉字（v15.14）**：设置开关；`numToCn` 支持十/廿/卅/卌/五十式（廿三世·勤）；角标/搜索徽标/统计条/帮助字辈表全链生效
- **谱书竖排·古法（v15.14）**：视图 ▾ →「谱书竖排（古法）」切换（带 ✓ 状态、刷新保留）：世代成行（行高按该行最高卡片自适应、`ROW_GAP_V=46`）、名字竖书（`writing-mode:vertical-rl`，右→左阅读序：名→女→嗣→字号→配偶）、**行左页边世数标**（取该行首个成员的字辈定代，第 0 行总根不标）；竖排下卡片隐藏代数角标/排行角标/备注行（信息进 tooltip），折叠钮移到卡片上缘避让「＋」；连线=父卡底→行间母线→各子卡顶；PNG/PDF 画布同款竖排（`drawNodeV` 逐字列绘 + 页边标）；拖拽换位/过继/搜索跳转/折叠等交互全部照常（nodeRects 数学与方向无关）

## 7. 交互约定与快捷键

| 操作 | 方式 |
| --- | --- |
| 打开操作菜单 | 点节点下方常驻「＋」/ **右键节点** |
| 改姓名 | 双击姓名 / 菜单「改名」/ F2 |
| 档案（生卒年/备注） | 菜单「档案」/ 双击姓名下方小字行 / Ctrl+I |
| 加配偶 | 菜单「＋配偶」/ **Ctrl+Shift+S（Ctrl+S 让位给保存）** |
| 改/移除配偶 | 双击图上配偶名字（清空=移除） |
| 加子女 | 菜单「＋子女」/ Tab |
| 加同辈 | 菜单「＋同辈」/ Enter（根节点用「＋ 第一代」） |
| 删除（含后代） | 菜单「删除」/ Delete（有确认弹窗，Ctrl+Z 可撤销） |
| 折叠/展开 | 菜单「折叠此支/展开此支」/ 点折叠支「▸ N」角标 |
| **撤销 / 重做** | Ctrl+Z / Ctrl+Y 或 Ctrl+Shift+Z |
| **搜索定位** | Ctrl+F / 🔍；Enter 下一个、Shift+Enter 上一个、Esc 关闭 |
| 保存 | **不存在保存动作**：编辑即自动保存到浏览器，无按钮、无弹窗；Ctrl+S 只提示「已自动保存」 |
| **备份 / 换电脑** | 「文件 ▾ → ⬇️ 备份到文件」下载 json；新机器「📂 恢复备份」选该 json（旧版 html 也可读） |
| 数据是否已固化 | 看顶栏状态灯：绿=已写回文件；橙=仅在本浏览器草稿（实时保存，刷新不丢） |
| 全展开/全折叠 | 顶栏「**视图 ▾**」下拉 |
| 导出/导入/打印/重排/重置 | 顶栏「**文件 ▾**」下拉 |
| 帮助（快捷键+数据说明） | 顶栏「?」按钮 |
| 拖拽换长幼 | 按住节点左右拖（独子也可拖） |
| **拖动过继（v14.2）** | 按住某人拖到某卡片上**停 0.4 秒**（亮琥珀光环）→ 松手成为 TA 的子女；拖到自己后代上=红框拒绝 |
| 缩放/平移 | Ctrl+滚轮（以光标为中心）/ 滚轮、触控板 / 拖空白 / 顶栏缩放组 |

> v14 设计原则：**顶栏只放高频与全局操作**（保存/撤销/搜索/缩放/统计），低频操作收进「视图」「文件」下拉，**针对单个成员的编辑全部在节点的「＋」菜单/右键菜单里**——顶栏不再重复堆叠编辑按钮。

## 8. 历史踩坑记录（排障时先看这里）

这些坑都真实发生过并被修复，接手者改代码时务必避免重蹈：

1. **`hidden` 属性被 CSS `display:flex` 覆盖** → 弹窗永远显示、关不掉。作者样式优先级高于 UA 对 `[hidden]` 的 `display:none`。**规则：显示/隐藏用 class（`.open`）控制，不要用 `hidden` 属性配自定义 display。**
2. **`opacity:0` 的元素仍可被点击命中** → 隐形按钮行拦截邻居节点点击。**规则：隐藏控件必须同时 `pointer-events:none`。**
3. **浮动层关闭逻辑放在 mousedown 且无条件执行** → mousedown 先于 click，菜单在 click 前被销毁，点击事件目标变成共同祖先 document，菜单项永远点不中。**规则：浮动层内按下必须直接 return，不触发任何关闭逻辑。**
4. **hover 才出现的按钮 = 反 UE** → 鼠标一动就消失、追不上。**规则：可点的东西要常驻可见**（现在「＋」常驻，半透明弱化，带 `::after` 扩展命中区）。
5. **布局算法把 `_w/_x/_y` 写进数据对象** → 草稿/文件混入布局垃圾。**规则：持久化必须过 `sanitize()`。**
6. **`sanitize()` 数组分支 `v.map(sanitize)` 把字符串拆成逐字对象** → `Object.keys("保壬新妹")` 变成 `{0:'保',1:'壬',…}`。**规则：sanitize 顶层先判 `typeof !== 'object'` 直接返回。**
7. **包围盒 `scan()` 不检查 `expanded`** → 折叠节点的子孙没有 DOM，任何含折叠节点的 render 直接 TypeError 崩溃。**规则：scan/layout/place 的递归必须与 createDom 一致，都看 `n.expanded`。**
8. **JS 注释/字符串里写字面 `</script>`** → 浏览器提前终止 script 标签，整个页面脚本语法崩溃。**规则：JS 里永远不出现字面 `</script>`，一律写 `<\\/script>` 或换措辞。**
9. **Playwright 测试坑**：
   - `page.evaluate("() => fn(); render();")` 是**定义未调用的箭头函数**，要写 `"() => { fn(); render(); }"`
   - 多个 `dialog` handler 会重复应答报 `already handled`，用单 handler + 可变变量
   - `:has-text()` 是 Playwright 专用选择器，页面内 `querySelector` 用不了
   - 拖拽测试的 mouseup 前要等 `suppressClickUntil`（350ms）窗口过去，否则点击被吞
   - 工作区会自动清理 `__` 前缀的临时文件，测试脚本用普通文件名（如 `t_check.py`）
   - `force=True` 点击会掩盖"元素被拦截"问题，关键交互必须用真实鼠标事件验证
10. **全局键盘快捷键泄漏进弹窗输入框（v12 真实 bug）**：document 级 keydown 只挡了 contentEditable，没挡 `<input>`。用户在档案弹窗里按 Tab 弹出「加子女」、按 Delete 弹出「删除确认」；姓名框回车保存后事件继续冒泡到 document，又触发「加同辈」第二个对话框。**规则：全局快捷键入口做分层守卫（搜索框 > 弹窗开着 > INPUT/TEXTAREA/contentEditable > defaultPrevented），且弹窗内回车保存要 stopPropagation。**
11. **display:none 下测量尺寸恒为 0（v12 真实 bug）**：`openMenu` 先读 `offsetWidth/Height` 再 `display='block'`，量出来永远是 0，只能靠兜底值定位。**规则：先显示再测量再定位（同帧内完成无闪烁）。**
12. **String.replace 的 replacement 是字符串时有 `$` 序列注入风险**：姓名若含 `$'`、`$&` 等序列，`saveToFile` 产出的 HTML 会被静默写坏；且正则要求内嵌 script 标签属性顺序/换行严格匹配，一旦不匹配会**无提示地下载旧数据**。**规则：用函数式 replacement `() => str` + 在 document 克隆上用 `querySelector('#__treeData')` 赋 textContent（不依赖属性顺序、不污染活动 DOM）。**
13. **双份初始数据必然漂移**：v12 在 JS 里维护 `initialData`、内嵌 JSON 又一份，手工同步。v13 已删除 JS 副本，`INIT_TEMPLATE` 直接从内嵌 JSON 深拷贝派生。
14. **overflow:hidden 视口让普通滚轮彻底失效**：v12 画布只能拖拽平移。v13 改 `overflow:auto` + 隐藏滚动条（`scrollbar-width:none` + `::-webkit-scrollbar{display:none}`），程序化 scrollLeft/scrollTop 平移照常工作。
15. **滚动容器内的悬浮层必须放在滚动容器外**：搜索框/tips 若绝对定位在 `overflow:auto` 的 viewport 里，会随内容滚走；sticky 在「唯一流内子元素」上也不成立。v13 用 `.canvas-wrap{position:relative}` 包住 viewport，悬浮层挂在 wrap 上。
16. **CDP 合成滚轮事件不带修饰键**：Playwright 的 `mouse.wheel()` 派发的 wheel 事件 `ctrlKey` 恒为 false，测「Ctrl+滚轮缩放」要注入 `new WheelEvent('wheel',{ctrlKey:true,...})`（测锚点数学）；真实受托事件路径由浏览器保证。
17. **测试断言里测「钳制类」行为要写全边界**：视口没有溢出时 `scrollLeft` 恒为 0，锚点缩放的期望值必须按 `max(0, min(需求量, 最大可滚动量))` 完整钳制公式计算，且测试要先保证画布比视口大（用小视口），否则无法区分「锚点正确」与「没滚」。
18. **全浏览器共用一个 localStorage 草稿键会跨文件污染**：v12/v13 的草稿键固定为 `zupu_tree_data_v3`，打开第二个族谱文件时会读到第一个文件的草稿并覆盖其内嵌数据。v14.1 改为键 = 键名 + 内嵌内容哈希（`zupu_tree_data_v3_<hash>`），每文件一个槽位；旧键保留只读迁移。教训：**持久化键要带文件身份**。
19. **FSA 句柄的可测试性**：真实 `FileSystemFileHandle` 是平台对象，可跨 IndexedDB 结构化克隆；但 JS mock 的假句柄带自有方法属性，`put()` 会抛 DataCloneError、克隆后方法全丢。测 restore/直写逻辑时：用 `add_init_script` 注入假选择器+活句柄（同页内不走 IDB），restore 的"重启恢复"用临时替换 `idbGet` 验证；哈希不匹配的拒绝逻辑一并覆盖。另外 headless 里点保存会走不到真弹窗，涉及下载断言的用例要显式 mock 成不支持（抛 TypeError）走兜底。

20. **给已绑定文件的"活页面"改盘上代码，会被旧页面回写覆盖**：v14 时代用户的浏览器标签页持有文件写句柄，页面内存里是旧代码+旧数据；此时改盘上的 HTML，用户下一次编辑就会用旧 buildSavedHtml 把整个文件（含新代码）写回。**规则：通知用户刷新（F5）后再继续编辑；发布新版本前后都要提醒。**
21. **Python 里重复包装 sys.stdout 会随机关闭底层流**：`sys.stdout = io.TextIOWrapper(sys.stdout.buffer)` 的旧包装器被 GC 时会关闭共享的 buffer，表现为测试脚本随机 "I/O operation on closed file"。**规则：统一用 `sys.stdout.reconfigure(encoding="utf-8")`，不要新建包装器。**
22. **批量补丁脚本里 `f.write(new)` 会整文件覆写**：一次手滑把 runner.py 覆盖成一行导致"全套通过"的假象（跑了个空脚本）。**规则：改文件一律 read→replace→write 回原内容；跑完测试先看输出是否为空。**
23. **`open(p,"w").write(open(p).read()…)` 会先截断再读，主文件被清空（v15.21 真实事故）**：一行式「写句柄 + 读原文」表达式，写句柄打开即把文件截为 0 字节，随后读到的就是空。**规则：改文件必须先把原文完整读进变量、在内存里替换、最后用 `with open(...,"w") as f: f.write(s)` 一次性写回；写前后各做一次长度/关键标记断言。**
24. **恢复预案（已演练成功）**：主文件意外损毁时，`tests/_out/fixture*.html` 是最近一次绿套件用**最新代码**重建的完整应用（仅内嵌数据被换成示例）；配合 `data.json`（真实数据+谱序+字辈快照，**每次改动数据结构后都要同步刷新**）即可完整重建主文件——先替换内嵌 JSON 为真实种子、再重放其后的小补丁、写前后断言、最后全套回归 + file:// 冒烟。注意 Python `len(str)` 数的是字符不是字节，中文 UTF-8 每字 3 字节，勿混用做阈值断言。
23. **先改数据后 `pushHistory` = 撤销永远无效（v15.12 才发现的潜伏 bug）**：档案弹窗保存一直把字段写完才推快照，快照里存的是已改完的状态，Ctrl+Z 恢复的仍是新值（生卒/备注/性别全中）。**规则：全项目约定「先 pushHistory 后改数据」（delete/addChild/rename 均如此），新动作必须遵守；无变化的保存不入栈。**
24. **无头 Chromium 对某些合成拖动像素路径会丢弃垂直滚动**：`mouse.move(steps=6)` 的拖空白平移，setter 被正确调用、回读也正确，但实际 scrollTop 恒 0；同样的坐标换到有头模式完全正常，且仅在画布内容变宽后出现（与栅格化路径相关，应用代码无辜）。**规则：拖空白平移测试的起点用左侧空白带（`width*0.30`），遇诡异「程序化滚动失效」先用有头模式复跑再怀疑代码。**

## 9. 验证方法

环境：系统 Python `C:/Python314/python.exe`（v13 时已 `pip install playwright` 成功，headless Chromium 用本机 `%LOCALAPPDATA%/ms-playwright` 缓存，无需下载）。

**标准回归（v13 起一键跑）**：
```bash
cd tests && C:/Python314/python.exe runner.py
```
覆盖 10 个模块 37 个用例，必须全绿才算交付：
- `t_core.py`：内嵌加载 / 节点·配偶·角标·统计条计数 / 真实点击选中 / 行内改名入撤销栈 / Ctrl+F 搜索开合与高亮计数
- `t_edit.py`：菜单项与实测定位（先显示后测量）/ 菜单加子女 / **弹窗键盘泄漏三连回归（Tab·Delete·回车保存后零多余对话框）** / 配偶增改删 / 删除确认统计全部后代 + 撤销重做全链
- `t_bookfmt.py`（独立夹具 fixture_book）：配偶谱书式显示与原名存储 / 边界（已含氏·复姓·单名）/ 搜索按原名 / 女性徽标显示与撤销 / 档案性别读写 / 世代基准 13→「23世·勤」全链（角标·tooltip·统计条·帮助表·搜索·持久化）/ 导出画布与 MD 同步
- `t_fold_drag.py`：折叠角标 ▸N 与点角标展开 / 根折叠 / 真实鼠标拖拽换位 + 角标更新 + suppressClick 窗口 / 未达阈值不入历史栈
- `t_reparent.py`：拖动过继 + 撤销 / 拖到自己后代红框拒绝 / 折叠目标自动展开 / 独子可拖
- `t_zibei.py`（独立夹具 fixture_zibei）：字辈匹配 11代·勤 / 推算 12代·修 13代·昭 / 冲突提示缺一代 / 抢救保留字辈 / 保存持久化 / 帮助字辈表
- `t_autosave.py`：刷新持久 / 跨页面 / 旧草稿抢救 / 备份下载 / html 导入 / 重置
- `t_export.py`：PNG/PDF 签名 / 折叠后更小
- `t_perf.py`：680 人渲染 <2.5s（实测 32ms）/ 单条连线 path / 全折叠搜索自动展开
- `t_view.py`：**缩放以光标为锚点（含上下界钳制公式）**/ 缩放上下限 / 普通滚轮与拖空白平移 / 打印文档树 13 人全覆盖 + PDF 冒烟

另有 `shots.py` 输出 4 张关键状态截图（初始/菜单/搜索/折叠）供视觉核对；`t_persist` 通过 http://127.0.0.1:8931 跑（先起 `python -m http.server 8931`），**交付前还需做一次 file:// 冒烟**（双击打开是产品主路径）：渲染、草稿、刷新恢复、下载均正常。

无头单测模板（需要临时写用例时）：
```python
from playwright.sync_api import sync_playwright
PATH = r"Z:\BaiduNetdiskWorkspace\myagent-work\zcode\legacy\index.html"
URL = "file:///" + PATH.replace("\\", "/")
with sync_playwright() as p:
    b = p.chromium.launch(headless=True)
    page = b.new_page(viewport={"width": 1400, "height": 900})
    errs = []
    page.on("pageerror", lambda e: errs.append(str(e)))
    page.goto(URL); page.wait_for_load_state("domcontentloaded")
    # ……断言……
    assert not errs
    b.close()
```
改完任何功能，至少跑一遍 `runner.py`；动过持久化/保存链路的话再补一次 file:// 冒烟。

## 10. 已知限制与下一步建议

- ~~打印缩放~~ → **v13 已修复**：打印改为生成文档式大纲（覆盖全部分支，无视折叠）
- ~~无"撤销/重做"~~ → **v13 已实现**：Ctrl+Z / Ctrl+Y，栈深 100
- ~~无搜索/定位~~ → **v13 已实现**：Ctrl+F 搜索高亮 + 循环跳转
- ~~数据无版本历史~~ → **v13 缓解**：保存到文件的文件名自动带日期时间（家族族谱-YYYYMMDD-HHMM.html），不再静默覆盖；真正的版本历史仍可考虑
- 搜索仅覆盖**当前可见分支**（折叠的支不参与）；如需全树搜索可先「全展开」
- 撤销栈是内存态：刷新页面后历史清空（草稿数据仍在，只是不可撤销）
- **触屏未适配**：双指捏合缩放、长按菜单等手势未做（桌面鼠标为主）
- **note 伪节点**：初始数据里 `雷聿（郝?）×徐海` 是虚线"待核对备注"节点（`id="note"`），不参与排行，若确认其辈分应移入正确分支
- **初始数据待核实**：`少秀（春?）`、`俞贵玄（美?）`、`周亿宁（?）` 等带 `?` 的字是手写识读结果，尚未确认
- 打印卡片在跨页边界仍可能被切开一行（HTML 分页固有限制，`page-break-inside:avoid` 只保证单人不裂）

## 11. 交付记录（版本时间线）

| 版本 | 内容 |
| --- | --- |
| v1 | jsMind CDN 版（首版空白 bug） |
| v2 | 重写为纯本地 HTML/CSS/JS |
| v3 | 家庭传代结构 + 肘形连线 + 自适应缩放 |
| v4 | 双击改字修复 + 结构化 {name, spouse, children} + 悬停按钮 |
| v5 | 多配偶（spouses 数组） |
| v6 | 拖拽排序长幼 + 排行角标 |
| v7 | localStorage 持久化 + 导入/重置 |
| v8 | 第一层加兄弟入口 + 悬停「＋兄」 |
| v9 | 档案（出生年/卒年/备注）+ 按生年自动排序 |
| v10 | 气泡菜单替代悬停按钮行 |
| v11 | **数据真源重构：内嵌 JSON + 保存到文件** + 排行称谓（长/次/三）+ 折叠此支 |
| v12 | 「＋」常驻 + 扩展命中区 |
| v13 | **深度重构（2026-08-27，接手 Agent）**：修复弹窗键盘泄漏 / 菜单定位 / 保存注入与属性顺序依赖 / 删除计数失真；删除 JS 双份初始数据；新增撤销重做、搜索定位、折叠人数角标、右键菜单、统计条；缩放以光标为锚点 + 滚轮平移；打印改文档式大纲；保存文件名带时间戳；**新增 tests/ 自动化回归套件（17 用例）**；视觉全面重设计（设计令牌、六代色环、点阵画布、卡片化节点） |
| v14 | **顶栏与界面重构（2026-08-27）**：20+ 平铺按钮收敛为单行顶栏（品牌区 + 撤销/重做/搜索图标 + 「视图」「文件」下拉 + 缩放组 + 统计 + 主操作保存 + 帮助）；SVG 图标替代 emoji；长副标题移入「帮助」弹窗（含数据保存说明与全快捷键表）；节点卡片细节打磨；测试同步改为 title 定位/下拉路径 |
| v14.1 | **持久化澄清与加固（2026-08-27，由用户"是不是实时保存"的疑问驱动）**：草稿键按文件内嵌内容哈希隔离（修多文件互相污染的设计缺陷，旧键只读迁移）；顶栏新增固化状态灯（绿=已固化 / 橙=未固化，点灯即保存）；帮助弹窗补充「实时保存 vs 固化」完整说明 |
| v14.2 | **拖动过继（2026-08-27，用户需求：把某人整支拖到另一个名字下当后代）**：拖到卡片上悬停 0.4s 亮琥珀光环 → 松手过继（排最幼、目标自动展开、可撤销）；拖到自己后代=红框拒绝防成环；独子也可拖（放开原"同辈≥2"限制）；命中检测用 nodeRects 世界坐标数学 |
| v14.3 | **保存=覆盖同一文件（2026-08-27，用户反馈"不要一堆下载的新文件"）**：集成 File System Access API 直写；取消时间戳文件名，兜底下载固定名「家族族谱.html」 |
| v14.4 | **Gmail 式无感保存（2026-08-27，用户反馈"逻辑太复杂、弹一堆说明"）**：删除绑定前的确认说明弹层——首次点保存直接弹文件选择器（仅一次原生交互）；例行保存零 toast，状态收敛为「已保存/未保存」两态；文件菜单精简为「🔗 绑定/更换族谱文件」；**测试套件改为夹具模式**（tests/_out/fixture.html = 示例数据副本，用户真实数据进文件后测试不再受影响）；数据快照（json/md）已同步为用户当前真实数据 |
| v14.5 | **授权恢复不再弹文件窗口（2026-08-27，用户反馈"点保存还是弹新窗口"）**：绑定过但浏览器重启导致授权过期时，点保存只触发浏览器的小气泡授权（requestPermission），绝不重弹文件选择窗口，授权后立即补写；有回归用例断言零 picker 调用 |
| v14.6 | **字辈定代（2026-08-27，用户提供 40 代字辈表）**：zibei 字段入数据模型；卡片左侧「代数·字辈」角标（匹配=蓝、推算=灰、缺代提示）；帮助弹窗字辈全表；统计条字辈范围；新增 t_zibei 模块 3 用例；runner 曾被误覆盖为一行、已重建并加 240s 子进程超时；stdout 改用 reconfigure 修 GC 关闭流问题 |
| v15 | **数据真源迁移到浏览器（2026-08-27，用户反馈"改了半天数据没了/不要弹窗另存/外部json也可以"）**：彻底删除 File System Access 绑定/写文件链路——数据每次编辑同步存入本浏览器 localStorage（zupu_data_v4），零弹窗零下载零文件写入；顶栏移除保存按钮，状态灯恒绿「已保存」，Ctrl+S 仅提示；首次载入从「文件内嵌/旧版草稿」挑成员最多者自动播种抢救；「文件」菜单=⬇️备份(json)/📂恢复(json+旧版html)/导出MD/打印/重排/重置；导入支持旧版 html 抽取数据；t_persist 重写为 t_autosave（5 用例），全套 28 用例 7 模块 |
| v15.1 | **播种补齐字辈表 + 全量去 emoji（2026-08-27）**：v15 载入若胜出者（旧草稿）没有 zibei 字段，自动从文件内嵌/其他候选补齐——修复用户"辈分角标不见了"；导入旧数据同样保留字辈表；全部 emoji 菜单项换为 SVG 线性图标（IC 表 + ic() 助手，菜单/文案统一风格）；修复 openDropdown 重复打开菜单项累积的潜伏 bug；新增 t_zibei 第 4 用例（旧草稿胜出时字辈表自动补齐），全套 29 用例 7 模块 |
| v15.2 | **推算代也显示字辈字（2026-08-27，用户要求）**：未用字辈命名的人角标同样带本代字辈（少群=「12·修」、豪=「13·昭」，灰色以示名字未采用）；tooltip 注明「本人名字未用本代字辈『修』」 |
| v15.3 | **角标带「代」+ 菜单图标对齐（2026-08-27）**：角标格式改为「11代·勤」；菜单图标与文字用 flex（.lbl）对齐并留 7px 间距 |
| v15.4 | **图谱导出 PNG/PDF（2026-08-27，用户需求）**：按布局数据在 canvas 重绘全树（标题带日期、连线、字辈角标 11代·勤、排行/出生年角标、折叠角标、生卒年备注；不受当前缩放/平移影响，高清 ≤2x、上限 8000px）；PDF 为内嵌 jpeg 的单页文档（页面尺寸=图谱尺寸，零依赖手写 PDF 结构）；「文件 ▾」新增「导出图片（png）」「导出 PDF（图谱）」，打印项改名「打印（纸质）」；新增 t_export 模块 3 用例（PNG/PDF 签名与内容、折叠态导出更小），全套 32 用例 8 模块 |
| v15.5 | **操作提示移到画布下方（2026-08-27，用户反馈悬浮提示遮挡）**：tips 从画布内悬浮条改为页面底部一行细字（精简文案，超长省略，悬停看全文；完整说明在「?」帮助）；画布高度相应调整 |
| v15.6 | **快捷折叠 + 搜索快跳 + 缩放流畅化（2026-08-27，用户三项需求）**：①有后代的卡片右侧常驻折叠切换钮（展开=「▾」一点收起、折叠=「▸ N」一点展开），无需进菜单；②搜索输入即出结果列表（带代数角标+备注摘要），点选或 ↑↓+Enter 直接跳到成员并居中选中；③滚轮缩放改倍速（×e^k，触控板小增量顺滑累加）+ rAF 插值动画 + 光标锚点全程保持，拖拽改缓存元素引用（零逐帧 DOM 查询），#stage 加 will-change 合成层；t_view 断言改倍速公式，全套 33 用例 8 模块 |
| v15.7 | **折叠钮不换行 + 配偶称谓制式（2026-08-27，用户反馈）**：折叠钮原先被追加在块级备注行之后导致掉行——改为插入姓名行内（有备注时 insertBefore(meta)）；配偶显示从「× 名」改为称谓标签「[原配 李平][续弦 丁嘉]」（spouseRole：原配/续弦/三房…八房，更多用第N房），卡片、图谱导出 PNG/PDF、打印大纲、MD 导出全部统一；新增称谓断言（t_reparent 内），全套 33 用例 8 模块 |
| v15.8 | **大族谱性能 + 搜索无盲区（2026-08-27，用户需求：几百上千人不卡、全折叠后搜索自动展开）**：①卡片交互改事件委托（每卡片零监听器）、DOM 构建与测量分离（批量测量，避免逐节点强制回流）、连线合并为单条 path（SVG 元素 O(n)→O(1)）、字辈每轮只算一次——680 人实测 render 32ms；②搜索改全树匹配，命中在折叠支内自动展开祖先链后再定位（不再有搜索盲区）；新增 t_perf 模块 2 用例（680 人渲染阈值 + 全折叠搜索自动展开），全套 35 用例 9 模块 |
| v15.9 | **生卒支持月日（2026-08-27，用户需求：生日加月日但不外显）**：出生/卒年字段自由书写（1951、1951-03-15、约1900、1920? 均可）；卡片备注行只显示四位年份（metaLine 年份化），完整日期进卡片 tooltip（生卒：…）、档案弹窗、搜索（可按月日搜）与备份/导出 json；按生年排序仍按四位年份解析；档案弹窗占位符同步提示写法 |
| v15.10 | **「周」字 favicon（2026-08-27）**：SVG data URI 内嵌（藏蓝底圆角块+白色宋体「周」），单文件原则不破坏、无外部文件 |
| v15.11 | **favicon 简化居中（2026-08-27，用户需求：去掉框、字绝对居中）**：删除琥珀描边内圈，只留藏蓝底；「周」改 `text-anchor=middle + dominant-baseline=central` 于 32,32 真居中（原 y=47 基线估中），字号 40→42；128/64/32/16px 预览验证均居中清晰 |
| v15.12 | **谱书式显示三件套（2026-08-28，用户出示真实谱书照片后提出）**：①性别字段——档案弹窗选「女」，卡片/搜索/打印/导出显示粉色「女」徽标（男不标，谱书惯例）；②配偶谱书式——显示层「丁嘉→丁氏嘉」（复姓/已含氏边界处理），数据存原名搜索不受影响，设置可关；③世代基准——设置里填字辈起始世数（13）即把「11代·勤」对齐谱书「23世·勤」，角标/搜索/统计/帮助/导出全链换算；配置存 `zupu_cfg_v1` 与数据分离。**顺带修复潜伏 bug：档案保存先改数据后 pushHistory 导致生卒/备注/性别从来无法撤销**；t_view 平移用例遇无头 Chromium 合成事件丢垂直滚动怪癖（有头正常，改用左侧空白带）。新 t_bookfmt 模块 4 用例，全套 10 模块 37 用例 |
| v15.13 | **姓氏显示开关（2026-08-28，用户补充需求）**：谱书显示设置新增「姓名显示姓氏周」勾选（默认开）；关闭后显示层剥除周姓（周勤善→勤善），与谱书「只报名」习惯一致——卡片/tooltip/prompts/搜索列表/打印/MD/画布导出全走 `dispName()`，数据存全名、搜索按全名命中；单字「周」与非周姓开头名字不剥 |
| v15.14 | **谱书四件套（2026-08-28，用户圈选可选项后追加竖排需求）**：①过继标记——档案「过继」下拉（嗣子=入继此支/嗣出=出继他支），卡片琥珀「嗣」徽标（嗣出为虚线描边），打印/MD/画布同步，tooltip 注明含义；②字/号字段——档案录入，卡片姓名后浅灰小字「字X 号X」，tooltip/搜索/打印/MD/画布全链；③世数用汉字——设置开关（numToCn：廿/卅/卌，廿三世·勤样式），角标/搜索/统计/帮助表全链；④**谱书竖排（古法）视图**——视图 ▾ 切换：世代成行（行高=该行最高卡片自适应）、名字竖书 `writing-mode:vertical-rl`（右→左阅读序）、行左页边标、卡片隐藏代数角标/排行/备注（进 tooltip）、折叠钮挂卡片上缘、连线改父底→行间母线→子顶、PNG/PDF 画布同款竖排（drawNodeV 逐字列绘+页边标）。**顺带修复：closeSearch 不交还焦点，隐藏输入框吞全局快捷键**。t_bookfmt 增至 7 用例，全套 10 模块 41 用例 |
| v15.15 | **竖排字辈行标 + deep review 修复（2026-08-28，用户反馈竖排字辈没显示）**：①行左页边标升级为「廿三世·勤」（世数+本代字辈字，**固定汉字数字**——阿拉伯数字竖排会逐位竖读成 2/3）；②deep review 修复：竖排导出画布字距与 DOM 对齐（1em 步进+宋体，防出框）、帮助弹窗过时的「Ctrl+S 保存到文件」改为自动保存说明、竖排插行线加长（130px）、搜索跳转按卡片半高居中（竖排高卡片此前跳偏）、竖排配偶称谓用谱书式「配/继配/三配」（`spouseBookRole`，横排保留原配/续弦）。对照欧式（横行表/五世一表）与苏式（垂珠吊线）体例完成调研，改进清单见交付说明。全套 10 模块 41 用例 |
| v15.16 | **谱序（堂号·源流）（2026-08-28，用户提供周氏整体资料：潮阳泗水周氏、申锡堂、周敦颐之后）**：根数据新增 `clan` 字段（ming 谱名/tang 堂号/chain 源流世系/yuanzu 先祖/shizu 始祖记/qianzu 始迁祖/origin 家族来源），随备份与导出走；**顶栏品牌区显示谱名+堂号徽标**（点品牌=打开谱序），标签页标题同步；视图 ▾ 新增「谱序（堂号·源流）…」编辑弹窗（7 字段，无变化不历史、可撤销）；PNG/PDF/打印/MD 标题自动用「谱名（堂号）」；载入时 clan 缺失自动从内嵌种子/旧草稿补齐（与字辈表同款救援）——**用户浏览器里的旧数据无需操作即获得谱序**。内嵌种子已写入申锡堂全套资料。t_bookfmt 增至 8 用例（t_clan_xu：载入/顶栏/编辑/导出标题/撤销/持久），全套 10 模块 41 用例 |
| v15.17 | **世系录导出（欧式五世一表）（2026-08-28，按调研建议①）**：文件 ▾ →「世系录（五世一表）」下载独立 HTML（宋体表格，浏览器打开即打印）：每人一行行传——世次（汉字谱世·字辈）/讳（名字按姓氏开关显示）/字/号/行第（与角标同规则：有生年按年、无生年按手动次序）/生/卒/配偶（称谓+谱书式名）/子女（女带标记）/记（嗣·止·备注）；按谱世每五代一表（「第廿一世至第廿五世」）；文首自动带谱序摘要（源流世系/先祖/始祖记/始迁祖/家族来源）；文件名=谱名-世系录-日期时间 |
| v15.18 | **过继加「兼祧」第三态（2026-08-28，按调研建议②）**：heir=jian（一子兼继两房）；徽标三样式——嗣子琥珀实底、嗣出琥珀虚线描边、兼祧蓝调双线描边；tooltip/「记」列/MD/打印/横竖排画布全链同步 |
| v15.19 | **「止」（无传）标记（2026-08-28，按调研建议③）**：档案勾选「止（无传）」→ 卡片黑圈「止」徽标（谱书凡例样式，手动标记不做自动推断）；tooltip 注明凡例含义；displayName/打印/MD/世系录「记」列/横竖排画布全链同步 |
| v15.20 | **配偶称谓细化（2026-08-28，按调研建议④）**：新 `spRoles` 按人覆盖称谓（配/继配/娶/聘/侧室；传统语义：配=初婚、继配=前妻亡故后再娶初婚女、娶=再醮妇、聘=定亲未娶），档案弹窗按配偶逐人生成称谓下拉（空=按次序默认原配/续弦；竖排默认配/继配）；migrate 清理越界项；卡片/tooltip/MD/世系录/横竖排画布全链生效，可撤销；**仅存覆盖项，不改 spouses 字符串数组（向后兼容）**。t_bookfmt 增至 12 用例，全套 10 模块 45 用例（当前版本） |
| v15.21 | **只有姓的配偶自动补「氏」（2026-08-28，用户反馈）**：`spouseDisplay` 只有姓时输出「黄氏」（原样返回「黄」）；复姓只有两字时整段视作姓（欧阳 → 欧阳氏，修复原逻辑会错切成「欧氏阳」）。**本轮补丁脚本曾把主文件截断为 0 字节，已从 tests/_out/fixture（v15.20 完整代码）+ 数据快照完整重建并全链验证，教训见 §8.23/24**。全套 10 模块 45 用例（当前版本） |
| v15.22 | **打印与 PDF 深检修复（2026-08-28，用户反馈效果不理想）**：①打印——`.canvas-wrap` 未在打印媒体隐藏，画布区留 ~800px 空白把正文顶到页尾（已隐藏）；打印版面重做：宋体、标题下加谱序摘要（源流世系/先祖/始祖记/始迁祖/家族来源）、每行前置「世次·字辈」徽标（无世次者显示行第）、名后随 女/嗣/兼祧/止 与 字号；②PDF/画布——分辨率 2x/8000px→3x/12000px、JPEG 0.92→0.95，画布宽度保证标题不被裁切（竖排窄画布曾裁掉谱名），树在加宽画布中居中；③**修复竖排导出画布与 DOM 布局不一致**：DOM 竖排是单列堆叠（名→女→嗣→止→字号→配偶列纵向排布），画布却按多列横排绘制导致文字画到卡外——drawNodeV 改为单列分段绘制、折叠钮位置对齐 DOM（卡上缘）；
④**竖排文字偏右真因（v15.23 末补，用户二次反馈后承认）**：单列重写时 `seg()` 漏设 `ctx.textAlign='center'`，每个字以左缘对齐中心线起笔，整列右移约半字宽——像素测量曾误报「居中偏差 0.5px」，因扫描把卡片上下边框横线一并算作文字（测量窗口两端必然有像素，中心恒等于窗口中心）。修复后内部逐字测量偏差 0.0px 并放大截图目检确认。**两条教训：导出画布必须与 DOM 布局逐像素对齐；暗文字扫描必须排除边框/框线并做放大目检，否则会得出虚假的居中结论**。全套 10 模块 45 用例（当前版本） |
| v15.23 | **导出对齐 + 谱书竖排装订打印（2026-08-28，用户反馈 PDF/PNG 姓名偏右、打印要参考图效果）**：①像素级排查（PIL 逐卡测量）证实竖排导出文字居中偏差仅 0.5px；真问题是横排画布字辈角标画在 x+2 而 DOM 在 x+14.5，角标戳出边框圆角、整体观感跑偏——已对齐（padX 13、角标 x+14）；②**打印跟随视图模式**：竖排视图下打印直接产出谱书竖排页（克隆舞台：竖书卡片+连线+行左世数标，去交互按钮，按 A4 纵向可打印区自动缩放，可装订成册），常规视图仍为文档大纲；@page 改纵向；竖排卡片样式选择器 `#stage.v` 改 `:is(#stage,.ps).v` 供打印克隆复用；③新增 `__ZP.rects()` 调试句柄。t_bookfmt 增至 12 用例（含竖排打印断言），全套 10 模块 45 用例（当前版本） |
| v15.24 | **谱书竖排「纸面化」CSS 重设计（2026-08-28，用户反馈框体喧宾夺主）**：竖排卡片去彩框去白底——透明底、墨色字（#29231c）、各代颜色只保留在行左世数标；始祖保留纸色底+细墨线文武框；hover/选中/搜索命中才现淡框；配偶/女/嗣/止小标全部改描边或去底；导出画布 drawNodeV 同步（非根节点不画框、墨字、根节点纸框）。**顺带修复：drawNodeV 姓名段参数传错（字体串传给 size），cy 被字符串拼接污染导致导出整卡文字消失**——seg 增加数值防御。横排视图不受影响。全套 10 模块 45 用例（当前版本） |
| v15.25 | **竖排视觉微调（2026-08-28，用户反馈：框别全去、要淡；名字加大；线调淡）**：竖排卡片恢复极淡框（rgba(60,50,35,.15)）+ 极淡纸底（rgba(255,253,248,.55)）；姓名 14.5→16px（画布 13.5→15，始祖 15.5/17px）；连线淡化 #a9b6c6→#d5dde6（DOM `#stage.v #lines path` 与导出画布同步，横排不变）；hover/选中边框相应提亮。全套 10 模块 45 用例（当前版本） |
| v15.26 | **竖排对齐强迫症审查（2026-08-28，用户反馈折叠钮没对齐）**：①折叠钮整体右偏 7.2px——基础样式 `.fold{margin-left:8px}`（横排行内用）在竖排绝对定位下仍然生效，把 `left:50%+translateX(-50%)` 的居中又推右 8px，竖排规则补 `margin-left:0`，实测偏差归 0；②行左世数标补 `text-indent:2px` 抵消 letter-spacing 的末字尾隙（视觉中心上移 1px 的强迫症项）；③逐项实测排除的疑似项：vrow 中心与各行卡片中心完全一致（283/422.9/592.5/725.2 分毫不差）、配偶 chip 距卡左缘 9px 无溢出（透明卡底造成的「悬浮」错觉）。全套 10 模块 45 用例（当前版本） |
| v15.27 | **竖排字体回退 + 加大（2026-08-28，用户反馈宋体不清楚）**：竖排卡片/行左世数标/导出画布全部从宋体（Songti SC/SimSun——Windows 上渲染发虚）换回应用默认字体（PingFang SC/Microsoft YaHei，`#stage.v` 与 `.ps` 打印克隆、`vrow`、drawNodeV、画布行标五处）；姓名 16→17px（始祖 18px，画布 16/16.5）；导出与屏显一致。全套 10 模块 45 用例（当前版本） |
| v15.28 | **折叠钮与名字拉开间距（2026-08-28，用户反馈）**：竖排折叠钮 top -11→-16px（完全悬于卡片上缘之外），卡片顶部 padding 9→13px 给首字留空气（钮底到首字约 11px）；横排折叠钮与名字间距 8→10px；导出画布折叠钮位置同步（y-6→y-12）。全套 10 模块 45 用例（当前版本） |
| v15.29 | **竖排配偶贴近名字（2026-08-28，用户反馈配偶离名字太远）**：竖排下配偶 chip 不再发前导空格（空格在竖排里占约半字高）+ margin-top 6→2px，名与配偶读成连贯一列（勤善配翁氏秀英的书式效果）；横排分隔空格保留。全套 10 模块 45 用例（当前版本） |
| v15.30 | **开源化（2026-08-28，用户决定以 MIT 单用户本地形态开源）**：①内嵌种子通用化——赵钱孙李演示谱（6 人，演示字辈匹配/女/字号/嗣子/生卒），周家真实数据归档 `samples/潮阳泗水周氏-示例.json`（可导入参考）；②**首次运行向导**——内嵌 demo 标记时自动弹三选一（先看示例 / 从空白开始 / 导入备份），空白开始支持谱名+堂号；③**修复：浏览器数据按成员数与内嵌比较导致空白谱刷新被示例顶掉**——现在浏览器数据一旦存在永远优先；④字辈表编辑器入「谱序」（空格/逗号/顿号分隔解析）；⑤姓氏自动识别（成员首字众数 ≥2），去周家硬编码；⑥导出文件名跟随谱名；⑦封面通用化（家族族谱·传代树，传家之谱印）；⑧README/CONTRIBUTING/CHANGELOG 开源化重写。新 t_wizard 模块 2 用例，全套 11 模块 47 用例 |

| v15.31 | **问题修复与文档一致性整理（2026-08-29，全仓库审查后集中处理）**：①**删除字辈/谱序「跨候选补齐」逻辑**——开源后内嵌种子是通用演示数据，用户主动清空的字辈表刷新后被演示字辈复活（t_zibei_cleared_stays_cleared 回归）；②**demo 标记随真实编辑摘除**（pushHistory 里 delete treeData.demo）——点背景关向导后录自家谱，刷新不再复扰（t_wizard_nag_stops_after_real_edit）；③**保存失败示警**——_lastSaveOk + save-fail 红灯「未能保存」，且 updateSaveState 移到 saveData 之后（顺序 bug：原指示灯显示的是上一次结果）（t_autosave_save_fail_visible 用 Storage.prototype.setItem 抛错模拟）；④favicon 周→族（通用化漏网）；⑤文档一致性：CHANGELOG 补 v15.31 且开源化归属改 v15.29–v15.30（原误标 v15.29）、README 徽章 v15.31、LICENSE zax→zaxchou、封面重制（v15.31/50 用例/2x）、.gitignore 去掉已改名文件的过时条目并新增 Thumbs.db 与 *-备份-*.json 防护（用户本机真实备份落在工作区，绝不入库）、删除过期快照 家族族谱-思维导图.md。新 3 用例改写 1 用例，全套 11 模块 50 用例（当前版本） |
| v15.32 | **多语言版本（2026-08-29，用户要求做英文/日文版并更新多语言 README）**：①`tools/build_i18n.py`——从中文母本 `index.html` 精确子串替换生成 `index-en.html` / `index-ja.html`，每条替换带次数断言，全部失配集中报告且不写文件（漂移探测器：母本演进后重跑即知漏翻）；②英文版：UI 全译，示例谱用**世代名**（Arthur/James/Robert/Michael，名字含世代名→字辈匹配照常工作），numToCn→阿拉伯数字、genLabel→「Gen N」、女→♀、嗣→ad、止→dsp、设置去汉字选项、bookSpouse 默认关；日文版：UI 全译，示例谱用单姓「林」家（保证姓氏识别/只报名语义正确），廿卅卌→二十三十四十，RANK_WORDS 長次三…，其余汉字概念（字/号/嗣/止/堂号）日文共用；③**CJK 守卫进母本**：spouseDisplay 遇非中日文名直接返回（罗马字名不再生成「X氏Y」乱码）；④两语言文件残留校验：EN 剔除注释后 CJK 仅剩 CS_SURN 复姓表与「氏」逻辑（数据级，保留）；⑤t_i18n 双冒烟（向导/添加成员/世代角标/顶栏图例无中文），runner 12 模块 52 用例；⑥踩坑：工具调用/Edit 里 `
` 是真实换行而表文件里是字面反斜杠+n，改续行缩进必须先 grep 实际字节；替换串吞结尾引号会造成 SyntaxError（addChild prompt 两语言各踩一次），语法错误用 window.onerror lineno 定位；⑦README 三语互链 + README.en.md / README.ja.md，Release zip 含三语言文件。⑧用户确认「不要语言切换，要独立语言单独文件」——应用本无切换器，但发现三文件共用 localStorage（中文数据会漏进英文界面），改为各语言独立存储键（zupu_data_v4_en/_ja + cfg 同理）并加测试断言。⑨封面也按语言分开：tools/build_covers.py 从中文源生成 _cover-en/_cover-ja（英文 Georgia 衬线 + FAMILY|TREE 书脊式双列竖标题 + ZUPU 印；日文明朝体 + 家系図竖题 + 家系図譜印），README 各挂各语封面。全套 12 模块 52 用例（当前版本） |
| v15.33 | **新增繁體中文版（2026-08-29，用户要求再加繁体中文）**：①`tools/build_i18n.py` 新增 build_hant——OpenCC s2twp 整文件转换（代码全 ASCII 不受影响，注释/示例谱一并转换保持四语同构），叠加独立存储键（zupu_data_v4_hant / zupu_cfg_v1_hant）、lang=zh-Hant、用语校正（賬號→帳號）；哨兵断言（儲存/檔案/字輩/家族族譜 必须出现、简体词必须消失）；②t_i18n 新增 zh-Hant 冒烟（嚮導/儲存燈/檔案選單/示例譜繁體/添加成員/獨立儲存鍵），53 用例；③封面四语言：build_covers.py 版本升 v15.33/53 用例（幂等 bump），新增繁体封面（PingFang TC/JhengHei + PMingLiU 明体，傳家之譜印），四张 PNG 重制；④README.zh-Hant.md（台湾用语全文）+ 四份 README 互链/徽章/下载指引/四语清单；⑤发布 v15.33：四个语言单文件资产（无完整包）。⑥用户反馈英文版不要古法纵排：英文封面改全横排（右侧 FAMILY/TREE 两行右对齐横排大标题替代书脊式竖排，迷你树名字牌改横排铭牌）；应用本体默认视图本就是横版（vertical:false），竖排（古法）仅作为可选项保留。⑦封面 v2（用户觉得旧版不够美，用豆包按我给的 prompt 生成了水墨挂牌图）：新管线 docs/_cover-v2.html——右侧裁用 AI 水墨艺术（_art-ink.png，左缘渐隐融入纸底），左侧 HTML 文字层（标题/红线/chips/版本/github），CSS 印章 132px 精确盖住底图乱码印；cover.png 已换成 v2 合成；注意 _cover-v2.html 内版本号是硬编码（v15.33/53），升版本时需同步；繁体版可复用同一底图（名牌本为繁体），英/日维持衬线版。⑧用户裁定：HTML 合成版不如 AI 原图，cover.png 直接用 AI 原图（_art-ink.png，2048×1024）；原图无版本号水印反而免维护，_cover-v2.html 留档不用。全套 12 模块 53 用例（当前版本） |
---

*交接完毕。有问题先看 §8 踩坑记录，再改代码；每改一处跑一遍 §9 回归。*
